
Dirty flag logic → Make sure you're not rebuilding meshes unnecessarily.

Frustum culling → Huge gain for minimal effort.

Greedy meshing → Major reduction in vertices.

Multithread terrain/mesh gen → Offload CPU-intensive work.

Later: Dynamic chunk streaming, LOD, or GPU occlusion culling.